Different pytest command:
1. pytest - will run all files of the form test_*.py or *_test.py in the current directory and its subdirectories
1. pytest -q name_file_test.py - run in quite reporting mode 